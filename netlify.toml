# JetLens is a single static page -- no build step, no functions, no secrets.
# Aircraft data comes straight from api.airplanes.live in the browser; see the
# comment above API_BASE in index.html for why there is no proxy.
[build]
  publish = "."
